# Assignment layout example

학생 repository에서 반복 과제는 다음처럼 분리하는 구성을 권장합니다.

```text
assignments/
├── a01/
│   └── solution.py
└── a02/
    └── solution.py
```

과제 등록 시 `assignment_path`에 `assignments/a01`처럼 정확한 상대 경로를 지정합니다.
